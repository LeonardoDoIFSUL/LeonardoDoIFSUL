const nome = "Rebecca";
var concatenacao = `O nome é ${nome} !`;
console.log(concatenacao);

// ${} -> Este comando serve para referenciar outra variavel fora da String quando é escrita com crase (``)...
//É como criar um furo no texto onde podem ser executado código, exemplo são contas matematicas...

console.log("A expresão seguinte esta craseada: " + `${1 + 1}`);